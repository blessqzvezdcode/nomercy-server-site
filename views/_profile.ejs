<!-- Admin Panel (if admin) -->
<% if (user && user.role === 'admin') { %>
<div class="admin-panel" style="position:fixed;top:80px;left:20px;z-index:1001;">
  <div class="admin-panel-content">
    <div class="admin-header">
      <span class="admin-icon">⚙️</span>
      <span class="admin-title">Админ панель</span>
    </div>
    <div class="admin-actions">
      <a href="/admin" class="admin-btn">Управление</a>
      <a href="/admin?tab=users" class="admin-btn">Пользователи</a>
      <a href="/admin?tab=items" class="admin-btn">Товары</a>
    </div>
  </div>
</div>
<% } %>

<!-- Moderator Panel (if moderator) -->
<% if (user && user.role === 'moderator') { %>
<div class="moderator-panel" style="position:fixed;top:80px;left:20px;z-index:1001;">
  <div class="moderator-panel-content">
    <div class="moderator-header">
      <span class="moderator-icon">🛡️</span>
      <span class="moderator-title">Панель модератора</span>
    </div>
    <div class="moderator-actions">
      <a href="/moderator" class="moderator-btn">Управление</a>
      <a href="/moderator?tab=pending" class="moderator-btn">Сделки</a>
      <a href="/moderator?tab=news" class="moderator-btn">Новости</a>
    </div>
  </div>
</div>
<% } %>

<div class="profile-widget card" id="profile-widget" style="position:fixed;top:80px;right:20px;width:280px;z-index:1000;">
  <% if (user) { %>
    <div style="display:flex;align-items:center;gap:12px;margin-bottom:16px;">
      <img src="https://cdn.discordapp.com/avatars/<%= user.id %>/<%= user.avatar || 'default' %>.png?size=128" style="width:48px;height:48px;border-radius:50%;" alt="">
      <div>
        <div style="font-weight:700;color:var(--neon);font-size:16px;"><%= user.name %></div>
        <div class="small" style="color:var(--muted)">ID: <%= user.id %></div>
      </div>
    </div>
    <div style="margin-bottom:16px;">
      <div class="small" style="color:var(--muted);margin-bottom:4px;">Баланс</div>
      <div class="neon-num" style="font-size:20px;"><%= balance %> NMCoin</div>
      <% if (user.xp !== undefined) { %>
        <div class="small" style="color:var(--muted);margin-top:8px;margin-bottom:4px;">Уровень</div>
        <div style="display:flex;align-items:center;gap:8px;">
          <span style="background:linear-gradient(135deg, #667eea, #764ba2);padding:4px 8px;border-radius:12px;font-size:12px;font-weight:bold;color:#fff;">
            <%= user.level || 1 %>
          </span>
          <span style="color:var(--neon);font-size:12px;"><%= user.title || 'Новичок' %></span>
        </div>
        <div class="small" style="color:var(--muted);margin-top:4px;"><%= user.xp || 0 %> XP</div>
      <% } %>
    </div>
    <div style="display:flex;flex-direction:column;gap:8px;">
      <% if (user.role === 'admin') { %>
        <div style="color:var(--neon);font-size:12px;text-align:center;margin-bottom:4px;">👑 Администратор</div>
      <% } else if (user.role === 'moderator') { %>
        <div style="color:var(--neon);font-size:12px;text-align:center;margin-bottom:4px;">🛡️ Модератор</div>
      <% } %>
      <a href="/profile/<%= user.id %>" class="btn ghost">Профиль</a>
      <a href="/logout" class="btn ghost">Выйти</a>
    </div>
  <% } else { %>
    <div style="display:flex;flex-direction:column;gap:8px;">
      <a href="/auth/discord" class="btn" style="text-align:center;">Войти через Discord</a>
    </div>
  <% } %>
</div>
